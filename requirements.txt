# 共通の runtime 依存。provider 固有のものは requirements-gcp.txt / requirements-aws.txt、
# 任意 plugin のものは requirements-optional.txt にある。開発は requirements-dev.txt を使う。
bottle~=0.13.2
google-auth~=2.57.1
requests~=2.34.2
argon2-cffi~=25.1.0
itsdangerous~=2.2.0
Pillow~=12.3.0
Arpeggio~=2.0.2
PyYAML~=6.0.2
# zoneinfo の fallback データ（base image に tzdata が無いときだけ使われる）。データだけなので常に最新でよい
tzdata>=2026.3
gunicorn~=23.0.0
